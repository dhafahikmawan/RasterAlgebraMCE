### Fix and Update List 05

### Update
1. For MCE, make it so the missing data handling mode `NaN` or `0` applies to all NoData calculations, regardless of it being originally NoData from the raster or as the result of the bounding box.